<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sweetk.dao">
    <select id="find_img" parameterType="String" resultType="ImgDividerDomain">
        SELECT * FROM img WHERE person_nm = #{person_nm}
    </select>
</mapper>